# Refinement Queue

Date: 2026-05-26

Status: Team 02 refreshed this refinement-only queue after reconciling the live runtime queue with the current Ready family. `CF-W2-DOV-01`, `CF-W2-SPL-01B`, and `CF-W1-RH-01A` are already routed and no longer belong in fresh refinement. The next unassigned stack is now headed by proposal-only consent-gated packets, with `CF-W1-MD-02A` first. Team 00 still owns Ready promotion and implementation routing.

## Active / Reserved Outside This Queue

These are real work items, but not fresh refinement-front candidates for this pass:

- `CF-W2-CAL-02A` remains in the routed calibration child path.
- Historical Today Review parents `CF-W1-TSC-02` and `CF-W1-TSC-03` are not fresh refinement-front candidates from current `dev`; their honest executable children were already prepared or consumed in earlier stacked work.
- `CF-W1-DQ-02-RS1` remains its own isolated DQ stream.
- `CF-W2-DOV-01` already has Team 08 UX planning, Team 03 architecture, and Team 04 pre-architecture QA scaffold output.
- `CF-W2-SPL-01B` already has Team 03 architecture plus Team 04 QA planning and is now a Team 00 promotion/sequencing decision.
- `CF-W1-RH-01A` is already promoted into the current Ready family.
- Accepted, parked, or already-routed items stay excluded from fresh refinement, including `CF-W1-BT-03`, `CF-W1-BT-04`, `CF-W1-CAL-01A`, `CF-W1-TP-01A`, `CF-W1-DQ-02A`, `CF-W1-HCTX-03`, `CF-W1-MCTX-02`, `CF-W1-STRAT-04`, `CF-W1-SQLAB-03`, `CF-W1-RH-03`, `CF-W1-RH-02A`, `CF-W1-SIG-LATEST-01`, `CF-W1-SIG-TRIGGER-ENTRY-01`, `CF-W1-SMI-01`, `CF-W2-SIG-01A`, and `CF-W2-BT-05`.

## Current Refinement Stack

| Rank | ID | Why it stays next | Team 00 routing note |
| --- | --- | --- | --- |
| 1 | `CF-W1-MD-02A` | Durable market-data evidence remains the strongest upstream provenance gap still outside active routing. | Keep blocked by schema/storage consent until Team 00 intentionally opens the packet. |
| 2 | `CF-W1-SQLAB-02B` | Durable Signal Quality learning memory remains the strongest measured-outcome follow-through proposal after the active lanes. | Keep blocked by storage consent until Team 00 intentionally opens the packet. |
| 3 | `CF-W1-STRAT-02B` | Durable strategy revision history still matters for exact rule/version auditability. | Keep blocked by schema/generated/repository consent until Team 00 intentionally opens the packet. |
| 4 | `CF-W1-L3-DQ-01A` | Passive Lane 3 readiness semantics remain a truthful DTO contract gap. | Keep contract-only until a bounded child can be defined without shared-file collisions. |
| 5 | `CF-W1-UX-01` residual parent | Research Workbench trust gaps still exist. | Keep behind the core direct-value trust stack. |

## Audit Notes

- `CF-W2-CAL-02` remains a valid parent requirement, but its bounded child `CF-W2-CAL-02A` is already routed and should not be re-queued as fresh refinement.
- `CF-W1-RH-01A` was the viable smaller no-storage follow-on surfaced by Team 01's Research Hub audit evidence, but Team 00 has already promoted it into the current Ready family.
- The queue now keeps active Team 06, Team 03, Team 07, and Team 05 work visible without pretending any of them are unassigned.
- The three storage-oriented proposals remain worth preserving in rank order, but they must stay explicitly consent-gated.
- `CF-W2-DOV-01` is elevated by explicit Product Owner direction and by the current mismatch between the nav label `Daily Overview` and the real launch-card-only `/` page, but it is no longer a fresh Team 02 refinement item because the UX and architecture packet already exists.
- Parent `CF-W1-TSC-02` and `CF-W1-TSC-03` remain useful historical product context, but they are not fresh refinement-front candidates from current `dev`.
- `CF-W2-SPL-01A` remains as the original over-broad child for split history only.
- `CF-W2-SPL-01B` is no longer a fresh Team 02 refinement item because Team 03 and Team 04 already completed the bounded backend-only gate packet; closed history stays deferred to a later durable lifecycle/storage child.

## Team 02 Constraint Reminder

- Do not move anything to Ready from this file.
- Do not treat active `CF-W2-CAL-02A`, `TSC-05A`, active `CF-W1-DQ-02-RS1` rework, or routed DOV/SPL gate work as unassigned discovery.
