# MD-A2 QA Evidence - Sync Catalog Progress And Bulk Performance - 2026-05-13

QA worker: MD-A2 QA Verification Worker  
QA plan: `docs/codex-agent-team-plan/qa-plans/2026-05-13-md-a2-sync-catalog-performance-qa-plan.md`  
Scope: read-only review of implementation plus automated and bounded live/local API evidence supplied by Orchestrator. Production source was not modified.

## Orchestrator Validation Evidence

| Evidence item | Status | Command/request | Key fields observed | Artifact link/path | Gaps or risk |
| --- | --- | --- | --- | --- | --- |
| Backend build | PASS | `backend npm.cmd run build` | Build passed per Orchestrator. | Orchestrator reported output | Output log not re-run by this QA worker. |
| Backend focused tests | PASS | `backend npm.cmd test -- market-data.service.test.ts market-data.routes.test.ts --runInBand` | Passed 2 suites / 94 tests per Orchestrator. Covered sync-run service and route registration. | Orchestrator reported output; reviewed `backend/tests/modules/market-data-foundation/market-data.service.test.ts` and `market-data.routes.test.ts` | Output log not re-run by this QA worker. |
| Frontend build | PASS | `frontend npm.cmd run build` | Build passed per Orchestrator. | Orchestrator reported output | Output log not re-run by this QA worker. |
| Frontend mocked UI tests | PASS | `frontend npm.cmd run test:ui -- market-data-foundation.spec.ts --workers=1 --output=test-results-md-a2` | Passed 8/8 tests per Orchestrator. Covered Sync Catalog start/progress/partial/cancel flows. | Orchestrator reported output; reviewed `frontend/tests/ui/market-data-foundation.spec.ts` | Output log not re-run by this QA worker. |
| Bounded live/local API check | PASS | `POST http://127.0.0.1:3000/api/market-data-foundation/stocks/sync-runs`, then `GET statusUrl` after 2 seconds | Start returned HTTP 202 in 124 ms for `IN/STOCK`, `batchSize=1`, `workerCount=1`, `workerConcurrency=1`, `maxBatches=1`; status poll returned HTTP 200 terminal `COMPLETED`, `processedCount=2905`, `skippedCount=2905`, `hasMore=false`, `percentComplete=100`, no recent errors. | Orchestrator reported live API output from 2026-05-13 | API evidence only; no browser screenshot attached. |

## Contract Evidence Table

| Requirement | Status | Evidence | Gaps or risk |
| --- | --- | --- | --- |
| Start endpoint exists and returns accepted sync-run contract | PASS | `backend/src/modules/market-data-foundation/market-data-foundation.router.ts` registers `POST /stocks/sync-runs`; controller returns `202`; service response includes `success`, `runId`, `status`, `region`, `assetType`, `scopeType=CATALOG`, counts, caps, timestamps, and `statusUrl`. Live start returned HTTP 202 in 124 ms with `runId=catalog-sync-20260513130353-xr3e8t`, `status=RUNNING`, `region=IN`, `assetType=STOCK`, `batchSize=1`, `workerCount=1`, `workerConcurrency=1`, `maxBatches=1`, `totalCount=2905`, `processedCount=0`, and the expected `statusUrl`. | None for start responsiveness. |
| Status endpoint exists and exposes progress/lost-run behavior | PASS | Router registers `GET /stocks/sync-runs/:runId`; controller returns status or `404` with `code=RUN_NOT_FOUND` and lost-state message. Frontend handles `RUN_NOT_FOUND` by showing lost/expired/restarted message and failed visible state. Live status poll after 2 seconds returned HTTP 200 terminal `COMPLETED`, `processedCount=2905`, `totalCount=2905`, `skippedCount=2905`, `hasMore=false`, `percentComplete=100`, no recent errors, and no-new-data message/warning. | I did not find a dedicated mocked UI test for `RUN_NOT_FOUND`; backend/controller static evidence exists. |
| Cancel endpoint exists and preserves run evidence | PASS | Router registers `POST /stocks/sync-runs/:runId/cancel`; service test `marks active catalog sync runs for cancellation without aborting the current batch` expects `cancelRequested=true`, `status=PARTIAL`, and the current-batch-finish message. | Live cancellation was not exercised manually. |
| Hard caps for bulk/provider work | PASS | Service clamps `batchSize` default/max `25/50`, `workerCount` `1/2`, `workerConcurrency` `2/3`, `delayBetweenBatchesMs` `3000` with `1000..30000`, and `maxBatches` `20/100`; service test `starts catalog sync runs quickly with hard caps and no immediate provider loop` verifies oversized inputs clamp to `50`, `2`, `3`, `1000`, `100`. Live bounded request used `batchSize=1`, `workerCount=1`, `workerConcurrency=1`, `delayBetweenBatchesMs=1000`, `maxBatches=1`. | Live run freshness-gated to no-new-data, so it did not exercise provider fetch concurrency under load; this is acceptable for the bounded live gate. |
| Bounded repository selection | PASS | `listActiveStockSyncTasks` accepts `take` and applies it to Prisma `findMany`; sync-run loop calls it with `run.batchSize` and excludes already processed IDs. Service test `processes only bounded catalog sync batches and reports partial progress/errors` verifies bounded batch behavior. | Static/test evidence only; no live DB query log captured. |
| No visible Sync Catalog call to `/stocks/sync-all` | PASS | Frontend API has new `startCatalogSyncRun` using `/market-data-foundation/stocks/sync-runs`. Mocked UI test `sync catalog starts a run, shows progress, and never posts sync-all` intercepts `/stocks/sync-all` and fails if posted. Legacy API helper still exists for compatibility but is not used by visible Sync Catalog evidence. | Static grep still finds legacy helper/route; acceptable only because visible UI test proves Sync Catalog does not call it. |
| Progress UI | PASS | UI renders alert titled `Catalog sync progress`, accessible progress bar `aria-label="Catalog sync progress"`, scope/status, batch size, provider concurrency, processed/total, succeeded, failed, skipped, no-op, rows inserted/updated/skipped, warnings, and batch chip. Mocked UI test asserts progress panel and request defaults. | No browser screenshot was generated by this QA worker. |
| Partial continue | PASS | UI shows `Continue catalog sync` when terminal `PARTIAL` has `hasMore=true`; mocked UI test `partial catalog sync offers continue for the same scope` verifies second start payload preserves `IN/STOCK`, `batchSize=25`, `workerCount=1`, and `workerConcurrency=2`. | Live partial continue not exercised. |
| Cancel UI | PASS | UI exposes `Cancel catalog sync` while active and posts `/sync-runs/:runId/cancel`; mocked UI test `active catalog sync can be canceled` verifies POST and visible `PARTIAL` cancellation message. | Live cancel not exercised. |
| Polling and post-terminal refresh | PASS | UI polling effect waits 1500 ms while active statuses are `PENDING`/`RUNNING`, stops on terminal statuses, and refreshes scheduler/catalog only after terminal status. Mocked UI test tracks status polls and scheduler refresh count. | Live network trace not captured. |
| Manual bounded live check | PASS | `POST http://127.0.0.1:3000/api/market-data-foundation/stocks/sync-runs` with `{ region: 'IN', assetType: 'STOCK', batchSize: 1, workerCount: 1, workerConcurrency: 1, delayBetweenBatchesMs: 1000, maxBatches: 1 }`, then `GET /api/market-data-foundation/stocks/sync-runs/catalog-sync-20260513130353-xr3e8t` after 2 seconds. | Start: HTTP 202, 124 ms, `RUNNING`, scoped `IN/STOCK`, bounded options echoed. Status: HTTP 200, `COMPLETED`, no-new-data terminal summary, no provider-heavy work run. | API evidence only; no browser screenshot attached. |
| Start responsiveness live check | PASS | Same bounded `POST /sync-runs`. | HTTP 202 returned in 124 ms, under the 2 second acceptance threshold. | None. |
| No long pending browser request live check | PASS | Same bounded live API check and status poll. | Start request completed in 124 ms; status was obtained by separate `GET` after 2 seconds; Orchestrator interpretation: no single API request remained pending for the catalog duration. | Evidence is API/network timing rather than browser DevTools screenshot. |
| Live status polling/progress/terminal summary | PASS | `GET statusUrl` after 2 seconds. | HTTP 200 terminal `COMPLETED`; `processedCount=2905`, `totalCount=2905`, `succeededCount=0`, `failedCount=0`, `skippedCount=2905`, `noOpCount=0`, `batchesExecuted=0`, `hasMore=false`, `percentComplete=100`, warning/message `No new data to ingest. Market session has no useful new daily data.`, `recentErrors=[]`. | Freshness gate meant no running provider progress snapshots were generated; terminal no-new-data summary is an accepted bounded live outcome. |

## QA Decision

Automated, static, and bounded live/local API evidence supports the implemented MD-A2 design: visible Sync Catalog starts `POST /api/market-data-foundation/stocks/sync-runs`, status/cancel routes exist, provider-facing work is bounded by backend caps, the UI shows determinate progress and counts, partial continuation is visible, cancel is visible, and the mocked UI test prevents regression to `/stocks/sync-all`.

QA signs off MD-A2. The previously missing bounded live/local evidence is now supplied: the start request returned in 124 ms, status polling produced a terminal no-new-data result, and the original start request did not remain pending for catalog duration. Final QA status is `SIGNED OFF`.
